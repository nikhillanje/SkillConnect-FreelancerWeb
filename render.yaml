services:
  - type: web
    name: skillconnect-freelancerweb
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py migrate --noinput
      python manage.py collectstatic --noinput
    startCommand: gunicorn SkillConnect.wsgi:application
